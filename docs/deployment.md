# Deployment

Placeholder — populated in Stage 8.

Target: GenLayer StudioNet, chain ID `61999`, RPC
`https://studio.genlayer.com/api`. Will document the deployment
command, the resulting contract address, and the inspected on-chain
schema.

# Credential Schema

Placeholder — populated in Stage 4 when identity evaluation and
credential issuance land.

Will document the `CredentialRecord` fields, the strict identity
verdict JSON schema, and the full positive/negative reason-code
allowlists (see build brief sections 9–11).

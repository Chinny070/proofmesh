# Security

Placeholder — expanded as write methods land.

Covers: prompt-injection protection for fetched evidence (build brief
section 17), allowlisted-source web retrieval rules (section 16),
deterministic pre-validation before LLM judgment, and the
non-deterministic leader/validator design for identity coherence,
continuity, and conflict adjudication (section 15).
